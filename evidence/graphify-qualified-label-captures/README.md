# Incremental label regression evidence

These PNGs are direct captures of the saved CLI evidence pages for the focused OpenWiki workload.

- `before.png`: Graphify v8 at `33362d9`, after the real rename/delete/import-rewire update, showing both file nodes as `errors.ts`.
- `after.png`: focused fix commit `ddab7f0`, with the same final source state and the qualified labels `generation/errors.ts` and `core/errors.ts`.

The graphs in both captures contain 197 nodes and 575 edges.
